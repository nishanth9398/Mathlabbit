from sympy import *

x,y,z=symbols('x,y,z')

f=exp(x+y+z)

I=Integral (f, (z,,x+log(y)), (y,,x), (x, 0, log(2)))

display(I)

I=integrate(f, (z,, x + log(y) ) (y,0,x), (x, 0, log(2)))

I=simplify(I)

display(I)
